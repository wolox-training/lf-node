const userFactory = require('./userFactory');
const weetFactory = require('./weetFactory');
const sessionFactory = require('./sessionFactory');
const ratingFactory = require('./ratingFactory');

module.exports = { userFactory, weetFactory, sessionFactory, ratingFactory };
